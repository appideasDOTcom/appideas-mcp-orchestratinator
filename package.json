{
  "name": "appideas-mcp-orchestratinator",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "description": "Shared MCP coordination server (mailbox + contracts + tasks + status dashboard) for coordinating Claude Code agents that live in separate VS Code windows.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "smoke": "node test/smoke.mjs",
    "test:admin": "node test/admin.mjs",
    "test:auth": "node test/auth.mjs",
    "test": "npm run smoke && npm run test:admin && npm run test:auth && npm run test:floor && npm run test:host && npm run test:window",
    "test:floor": "node test/floor.mjs",
    "test:host": "node test/host.mjs",
    "host": "node host/index.js",
    "test:window": "node test/window.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "better-sqlite3": "^11.8.0",
    "express": "^4.21.2",
    "zod": "^3.24.1"
  }
}
