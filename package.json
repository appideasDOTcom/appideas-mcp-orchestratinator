{
  "name": "appideas-mcp-orchestratinator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared MCP coordination server (mailbox + contracts + tasks) for coordinating Claude Code agents that live in separate VS Code windows.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "smoke": "node test/smoke.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "better-sqlite3": "^11.8.0",
    "express": "^4.21.2",
    "zod": "^3.24.1"
  }
}
